// src/components/inner-header.tsx
// Shared chrome on inner pages: same wordmark, menu, and socials.

import { SiteHeader } from "./site-header";

export function InnerHeader() {
  return <SiteHeader tone="light" />;
}
