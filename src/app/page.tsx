// src/app/page.tsx
// Home is the persistent hero. This page only locks scroll.

import { HomeScrollLock } from "@/components/home-scroll-lock";

export default function HomePage() {
  return <HomeScrollLock />;
}
